const filterPrioritized = require('.');

describe('filterPrioritized', () => {
	test('Initial test.', () => {
		const result = filterPrioritized();

		// eslint-disable-next-line no-console
		console.log(result);
	});
});
